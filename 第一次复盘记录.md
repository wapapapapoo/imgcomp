# 我的复盘内容

## 本次学习内容：

本次学习期间，我按照专题要求，首先粗略地阅读了五篇初级论文，对深度学习图像压缩这个方向建立初步的认识。粗读后，我发现五篇入门文章呈现很清晰的脉络，balle2016建立了lic的范式；balle2018提出hyperprior，认为隐变量的方差在空间上不平稳，用超先验来对不同空间位置指定不同的方差，进而分配不同的编码长度；minnen2018引入了一个context model，压缩了局部相邻像素的信息冗余；mentzer2020引入gan，用图像生成的思路来做压缩，并指出失真和感知也存在矛盾，已有的metric与人类真实感受之间有差距；he2021则把以前只能串行执行的自回归context model变成了棋盘格，先不用context重建一半的像素，再用context重建另一半像素，大幅提高了时间效率。

同时，我准备进一步精读这些文章，并撰写一份综述。目前已经读完了balle2016，着重了解了它将量化变得可微，以及设计率失真loss的过程。综述已经写完的部分位于：https://github.com/wapapapapoo/imgcomp/blob/main/survey.md ，完成的段落包含jpeg png等传统方法和balle2016的部分。以下是摘抄的部分段落：

……

balle2016

Balle et al. 提出了一种端到端优化的图像压缩方法[^balle2016endtoend]，是深度学习图像压缩的开山之作。他们借鉴了 JPEG 变换-量化-编码的范式，使用一对小网络$g_a$, $g_s$对图像做非线性变换，取代了传统方法中人工设计的线性变换，呈现一种类似 AE 的结构: $$
y=g_a(x;\phi),\\
\hat{x}=g_s(\hat{y};\theta).
$$ 实现端到端图像压缩还需要使量化步骤可微。一种容易想到的方法是重采样取整时的差分，即 $$
\hat{y} = \lfloor y \rceil = y + \operatorname{detach}(\lfloor y \rceil - y),
$$ 易知 $$
(\lfloor y \rceil - y) \in (-0.5, 0.5].
$$ 作者则使用了更稳健的方法，直接使用一个能够覆盖这一量化间隔的均匀噪声，模拟了量化过程：$$
\tilde{y} = y + \Delta,\quad \Delta \sim \mathcal{U}(-0.5, 0.5).
$$ 此时，$\tilde{y}$ 的分布成为一个以真实量化值 $\hat{y}$ 为中心、宽度为 $1$ 的均匀分布混合体。评估时则直接舍入成 $\hat{y}$，得到离散的隐变量，进行实际的编码与解码。

为了训练这个模型，作者使用了 Rate-Distortion 损失 $$
\begin{aligned}
\mathcal{L} &= R + \lambda D,\\
D &= \operatorname{MSE}(g_p(x), g_p(\hat{x})),\\
R &= -\sum_{i}^{C' H' W'} \log p_{\tilde{y}_i}(\tilde{y}_i) = \mathbb{E}[-\log p_{\tilde{y}}(\tilde{y})] \approx \mathbb{E}[-\log P_{\hat{y}}(\hat{y})] = H(\hat{y}),
\end{aligned}
$$ 其中 $D$ 表示失真项，衡量重建图像 $\hat{x}$ 与原始图像 $x$ 的差异。作者意识到直接对 $x$ 和 $\hat{x}$ 取 MSE 可能导致模型学习噪声，且单纯 MSE 不能根据人眼对图像信息的感知能力做取舍，故而提出使用一个 $g_p$ 变换将图像变换到感知空间，再进行 MSE (尽管原文自己的实现直接令 $g_p(x) = x$，仅在 discussion 和插图中进一步讨论)。$R$ 表示码率项，作者希望最小化 $\hat{y}$ 的平均码长，然而因为 $\hat{y}$ 离散，难以估计，转而使用连续的 $\tilde{y} = \hat{y} + \Delta$ 进行近似。又由于连续概率密度函数 $p_{\tilde{y}_i}$ 未知，因此作者将其参数化为分段线性函数 (线性样条) $$
p_{\tilde{y}_i}(\tilde{y}_i; \psi_c), \quad i = (b,c,h,w),
$$ 与模型同时训练。

此外，文章中还借鉴生理学，引入了 GDN 和 IGDN 学习通道间的促进和抑制作用；以及复刻了 CABAC，实现了一个基于 $p_{\tilde{y}_i}$ 结果的算术编码。这些内容共同组成了一套完整且影响深远的框架。它通过一对可类比为 VAE 的非线性变换加噪声模拟的量化，消除冗余，转换为更易于压缩的隐变量，再对隐变量进行自适应的编码，从而产生具有体积和质量优势的压缩形式。

理想情况下，$g_a$, $g_s$ 是完美的变换，那么隐变量 $y$ 将能彻底消除通道、位置间的依赖，每个维度将表示独立的、纯粹的信息，从而彻底消除冗余。然而，在实际实现中，$g_a$, $g_s$ 必然受到模型归纳偏置的影响，且隐变量 $y$ 依然不完美，各个维度并非独立，依然保留了空间和通道之间的大量依赖。此时若再坚持认为 $\tilde{y}_i$ 各自独立同分布，将它们视为孤立的事件进行编码，将无法取得更好的效果。因此，后续的工作采用了 hyperprior [^balle2018variational], context model [^minnen2018joint], 以及 channel-wise context model [^he2022elic] 等方法来进一步压缩空间、通道、语义上的冗余。

……

## 下次学习计划：

继续精读剩下的四篇文章。计划周末读完balle2018和minnen2018，并更新我的综述笔记。

# 评语

本次得分92分 Codeforces积分1561

## 内容的翔实程度 100分

报告对学习内容的阐述极为具体和深入。学生不仅列出了五篇核心论文，还清晰地梳理了它们之间的演进脉络（如从balle2016的范式建立到he2021的棋盘格改进）。对balle2016的精读部分尤为翔实，详细解释了量化可微的处理方法（均匀噪声模拟）、率失真损失的设计、GDN的引入等，并准确引用了关键公式和术语。综述链接中的摘录段落进一步证明了其理解的深度和细节的准确性。

## 关联度 100分

本周工作内容与学习方向“基于深度学习的图像压缩”高度契合。学生明确围绕该专题进行学习，阅读的五篇论文均为该领域的经典入门文献，从基础范式到最新效率改进，构成了一个完整的学习路径。撰写综述笔记也是对该方向知识的系统梳理和巩固，所有活动都直接服务于核心研究方向，无需额外解释关联性。

## 工作量 80分

报告反映的工作量充实且质量高。学生在一周内完成了对五篇初级论文的粗略阅读以建立整体认识，并对其中一篇（balle2016）进行了精读，深入理解了关键技术细节。同时，已开始撰写综述笔记，并完成了部分内容（包括传统方法和balle2016）。这体现了从广度到深度的合理过渡，工作量足以支撑一周的投入。精读一篇并撰写详细综述段落，其钻研深度符合当前阶段的要求。

## 规划性 80分

学生对下周的计划清晰、具体且可执行。计划明确指出要继续精读剩下的四篇文章（balle2018和minnen2018），并更新综述笔记。这直接延续了本周“精读文章并撰写综述”的工作主线，体现了良好的自我驱动和对研究进程的规划。计划虽然具体，但若能进一步细化（例如，计划每天阅读多少，或重点关注论文的哪些部分），可使其更具操作性。

## 批判性思考 100分

报告展现了出色的批判性思考和知识吸收能力。学生在梳理论文脉络后，不仅客观叙述，还主动指出了技术演进的内在逻辑（如后续工作针对隐变量依赖问题的改进）。在精读balle2016的部分，不仅复述内容，还分析了作者设计（如感知空间变换的讨论与实际实现的差异）并指出了模型的局限性（隐变量并非真正独立，存在冗余），这体现了对所学内容的深刻理解和主动思考。
